# TX-Observer | Python dependencies
# Install: pip install -r requirements.txt

# Environment / configuration
python-dotenv>=1.0.0

# Market data — Yahoo Finance (台指期 WTX=F)
yfinance>=0.2.40

# Data handling
pandas>=2.0.0
numpy>=1.24.0

# Chart rendering (headless — no GUI required)
mplfinance>=0.12.10b0
matplotlib>=3.7.3

# HTTP client — Imgbb upload + LINE Messaging API push
requests>=2.28.0

# Scheduler
APScheduler>=3.10.0

# Timezone support
pytz>=2023.3
