# ─────────────────────────────────────────────────────────────────────────────
# TX-Observer | Environment Variables Template
#
# 使用方法：
#   cp .env.example .env
#   # 用文字編輯器開啟 .env，將所有 <...> 替換為你的真實金鑰
#
# 注意事項：
#   - 填好的 .env 已被 .gitignore 排除，絕對不會被 git commit
#   - 本檔案 (.env.example) 只含欄位名稱與說明，可安全提交至 GitHub
#   - 所有金鑰請妥善保管，勿截圖、勿貼入公開聊天室
# ─────────────────────────────────────────────────────────────────────────────


# ── Shioaji API (永豐金證券) ───────────────────────────────────────────────────
#
# 取得方式：
#   1. 前往 https://sinotrade.github.io/ 申請開通 API 權限
#   2. 登入永豐金 e-Leader → API 管理 → 建立金鑰
#   3. 取得 API Key 與 Secret Key（各約 44 字元）
#
SHIOAJI_API_KEY=<your_shioaji_api_key>
SHIOAJI_SECRET_KEY=<your_shioaji_secret_key>


# ── Discord Webhooks ──────────────────────────────────────────────────────────
#
# 每個監控品種對應一個獨立的 Discord 頻道 Webhook URL。
#
# 取得方式：
#   1. 在 Discord 伺服器中，對目標頻道點右鍵 → 「編輯頻道」
#   2. 進入「整合」→「Webhook」→「建立 Webhook」
#   3. 複製 Webhook URL（格式：https://discord.com/api/webhooks/...）
#
# TX  = 台指期近一 (TXFR1)
# TSE = 加權指數 (TSE/001)
# OTC = 櫃買指數 (OTC/101)
#
DISCORD_WEBHOOK_TX=<your_discord_webhook_url_for_txfr1>
DISCORD_WEBHOOK_TSE=<your_discord_webhook_url_for_tse>
DISCORD_WEBHOOK_OTC=<your_discord_webhook_url_for_otc>


# ── Telegram Bot ──────────────────────────────────────────────────────────────
#
# 使用 Telegram 超級群組（Supergroup）的論壇主題（Forum Topics）推播。
# 每個品種推送至各自的 message_thread_id（主題 ID）。
#
# 取得 TELEGRAM_BOT_TOKEN：
#   1. 在 Telegram 搜尋 @BotFather
#   2. 傳送 /newbot，依指示建立 Bot
#   3. 複製 Token（格式：123456789:ABC-DEF...）
#
# 取得 TELEGRAM_CHAT_ID：
#   1. 將 Bot 加入目標超級群組並賦予發送訊息權限
#   2. 傳送一則測試訊息給 Bot
#   3. 呼叫 https://api.telegram.org/bot<TOKEN>/getUpdates
#   4. 從回應中取得 chat.id（超級群組為負數，格式：-100xxxxxxxxxx）
#
# 取得 TELEGRAM_THREAD_TSE / OTC / TX（論壇主題 ID）：
#   1. 在超級群組中建立論壇主題（需先開啟 Topics 功能）
#   2. 傳送訊息後，從 getUpdates 回應的 message_thread_id 欄位取得
#
TELEGRAM_BOT_TOKEN=<your_telegram_bot_token>
TELEGRAM_CHAT_ID=<your_telegram_supergroup_chat_id>
TELEGRAM_THREAD_TX=<message_thread_id_for_txfr1>
TELEGRAM_THREAD_TSE=<message_thread_id_for_tse>
TELEGRAM_THREAD_OTC=<message_thread_id_for_otc>
